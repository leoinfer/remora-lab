# External benchmark data

This directory intentionally contains no machine telemetry, model metadata,
weights, raw run receipts, or calibration captures. Supply a hardware profile
and calibration anchors from a separately reviewed experiment when using the
research tool. The CLI accepts any profile with the schema implemented by
`lb-hardware`.
