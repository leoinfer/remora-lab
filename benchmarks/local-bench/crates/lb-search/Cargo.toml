[package]
name = "lb-search"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Config-space search: beam search over ngl/threads/batch/ctx/KV/FA/MTP with fit constraints, scored on a workload."

[dependencies]
serde.workspace = true
serde_json.workspace = true
lb-model = { path = "../lb-model" }
lb-hardware = { path = "../lb-hardware" }
lb-sim = { path = "../lb-sim" }
