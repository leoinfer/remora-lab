[package]
name = "lb-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Synthetic performance simulation: decode/prefill prediction from model metadata + hardware phenotype + config. No model execution."

[dependencies]
serde.workspace = true
serde_json.workspace = true
lb-model = { path = "../lb-model" }
lb-hardware = { path = "../lb-hardware" }
