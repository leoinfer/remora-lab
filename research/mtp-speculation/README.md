# MTP and speculative decoding research

The research tracks draft proposals, target verification, accepted tokens,
rejected work, and memory/transfer cost. The public Rust scheduler exposes
these as explicit telemetry. It does not assume that a larger draft horizon
or a high acceptance ratio produces a speedup.
