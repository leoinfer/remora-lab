# Missing data and negative evidence

No real bounded K/V dump, recurrent/GDN/SSM state dump, or attention trace with
source hash was found in the searched local project areas. Existing GGUF files
are model weights, not captured runtime state; they were not opened or loaded.
A large DeepSeek cache-test log exists but was excluded from memory-intensive
processing and is not evidence of an authority KV fixture.

Consequences:

- lossless codec ratios are synthetic-only;
- no claim of total-memory compression is allowed;
- no promotion accuracy, attention concentration, MTP acceptance, or cold-tier
  reuse distribution is empirically measured;
- certificate success rate and replay throughput remain sensitivity inputs;
- no live GPU or end-to-end test is authorized while the main lane owns the
  runtime.

The minimum future capture is specified in
`CONTEXTFOLD_MINIMUM_CAPTURE_PLAN.md`; it must be performed in an isolated
worktree/session after the main GPU lane releases the required gate.
