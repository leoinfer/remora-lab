[package]
name = "har-plan"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Immutable execution plans, plan importer import and validation"

[dependencies]
har-core = { path = "../har-core" }
har-ir = { path = "../har-ir" }
har-memory = { path = "../har-memory" }
har-model = { path = "../har-model" }
serde.workspace = true
serde_json.workspace = true
