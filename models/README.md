# Models

Model files are never stored in this repository. Users supply a compatible
model path separately and should follow that model's own license and
distribution terms. Public tests use synthetic data or small reviewed
fragments only.
