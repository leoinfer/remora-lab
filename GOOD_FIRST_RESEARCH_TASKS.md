# Good first research tasks

- Add a public synthetic GGUF generator and a model-free end-to-end smoke test.
- Port the publication audit to a CI job that also checks dependency licenses.
- Independently review the R4KV byte layout and add cross-language test vectors.
- Add a Vulkan shader compilation check without checking in generated binaries.
- Build a reproducible CPU-versus-Vulkan quantization error harness.
- Turn the effective-context accounting notes into a model-free simulator.
- Add a public speculative-decode benchmark with both accepted and rejected
  token accounting.
- Review the omitted Laguna and HAR-X sources and make an explicit license
  decision for each file.
- Recover exact upstream citations for the two unconfirmed prior-art labels in
  `PROVENANCE.md`, or remove those labels entirely.
